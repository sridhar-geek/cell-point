import React from "react";
import ProductCard from "./productCard";

const NonPriroritySection = () => {
  /* Get the categories and create selection tab 
   send request to get the products which are not in prirority category
     Join two tables where category is non prirority or false
    get those products with those categories
    use groupBy function and then map those products
     */
  return (
    <div>
      NonPriroritySection
      {/* <ProductCard /> */}
    </div>
  );
};

export default NonPriroritySection;
